# 项目规则

## 元服务开发强制验证

- 本规则适用于当前工程的元服务产品（`atomic`）及其入口模块（`products/atomic_entry`），也适用于所有会影响元服务页面的公共模块、业务模块和组件。
- 每次新增、修改或修复代码后，必须在 DevEco Studio 中重新编译元服务，并重新启动到 HarmonyOS 模拟器；不得仅凭静态检查、命令行编译或热重载结果判定完成。
- 启动成功后，必须在模拟器中进入本次改动对应的页面，检查页面能否正常打开、内容和布局能否正常显示、相关交互能否正常使用，并确认无崩溃及明显运行时错误。
- 一次改动影响多个页面或关键流程时，必须逐一检查所有受影响页面及主要流程。
- 完成任务时必须说明 IDE 编译启动结果、已检查的模拟器页面和检查结果。
- 若当前环境无法操作 DevEco Studio 或模拟器，必须明确说明尚未完成上述验证，不得声称任务已完整完成或页面已正常。
