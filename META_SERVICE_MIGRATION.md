# 元服务迁移审计与实施方案

## 当前结论

原 `products/entry` 是完整普通应用入口。直接把它编译为元服务会触发约 542 个 `11706010` 限制错误，因此不能通过改包名或签名完成迁移。原入口和全部页面保留不动；新增 `products/atomic_entry` 作为元服务入口，并使用 `atomic` product 构建。

## 功能矩阵

| 功能 | 当前实现 | 元服务状态 | 迁移方案 |
|---|---|---|---|
| 首页、寄件、订单、优惠、我的页面 | ArkUI 多模块 | 可保留但需逐页编译验证 | 从无受限依赖的页面开始逐页接入 atomic_entry |
| 本地订单/地址/模板/优惠数据库 | `@kit.ArkData` relationalStore | 当前不可原样保留 | 全部改为现有 Java 服务器 HTTPS API；客户端仅保留轻量偏好设置 |
| 微信/支付宝登录 | 原生 HAR SDK | 不保留 | 改为华为账号登录/服务授权，Java 后端统一账号绑定 |
| 微信/支付宝支付 | 聚合支付 HAR | 不保留 | 改为华为支付/Huawei IAP，Java 服务端完成订单校验与发货 |
| 音频、语音识别、文字识别 | `AudioCapturer` 等 | 当前编译不支持 | 改为服务端能力或元服务支持的 Kit；无法替换的功能保留在普通应用 |
| 地理编码 | `geoLocationManager.getAddressesFromLocationName` | 当前编译不支持 | 使用 Map Kit 已支持的站点检索；必要时后端地理编码 |
| 地图/位置 | Map Kit、Location Kit | 部分 API 需逐项验证 | 只引入元服务白名单 API，并在真机验证权限 |
| 推送/服务动态 | Push Kit 现有代码 | 需 AGC/权限配置 | 接入服务动态时间线与 Push Kit，发布前做 AGC 配置 |
| 统一扫码 | 尚未接入 | 可作为独立能力接入 | 使用 Scan Kit，避免依赖数据库和原生支付模块 |
| 服务直达/服务卡片 | 快捷方式、Form | 需按元服务规范调整 | 保留服务卡片，增加 instant service 入口配置 |
| 智能填充 | 尚未接入 | 需能力与权限审核 | 先实现表单字段映射，再做真机验证 |

## 迁移顺序

1. `atomic_entry` 构建、签名和真机启动。
2. 网络 API、华为账号、华为支付基础链路。
3. 首页与寄件表单页面逐页接入。
4. 订单、地址、优惠改为后端数据源。
5. Scan Kit、Map/Location Kit、服务直达、服务动态、智能填充逐项验收。

## 当前明确不能原样保留

本地关系型数据库访问、当前微信/支付宝原生 SDK、当前音频/语音/文字识别实现、`getAddressesFromLocationName` 地理编码调用。它们必须替换或降级为普通应用专属能力，不能在元服务构建中继续直接引用。

## Java 服务端改造输入

元服务端只依赖 HTTPS API。后续接入前需要为 Java 服务补齐或确认：华为账号登录换取业务 Token、模板/地址/订单/优惠/会员查询与写入、华为支付下单与回调验签、物流查询、扫码结果查询、服务动态推送 token 绑定。接口地址、鉴权方式和字段确定后，再逐项替换客户端 Repository，不再恢复本地关系型数据库。
